<header class="modal-header">
    <h1>添加订阅者</h1>
</header>
<a class="close icon-x" href="" title="Close" {{action "closeModal"}}><span class="hidden">关闭</span></a>

<div class="modal-body">
    <fieldset>
        {{#gh-form-group errors=model.errors hasValidated=model.hasValidated property="email"}}
            <label for="new-subscriber-email">电子邮件地址</label>
            <input type="email"
                value={{model.email}}
                oninput={{action "updateEmail" value="target.value"}}
                id="new-subscriber-email"
                class="gh-input email"
                placeholder="电子邮件地址"
                name="email"
                autofocus="autofocus"
                autocapitalize="off"
                autocorrect="off">
            {{gh-error-message errors=model.errors property="email"}}
        {{/gh-form-group}}

    </fieldset>
</div>

<div class="modal-footer">
    <button {{action "closeModal"}} class="btn btn-default btn-minor">取消</button>
    {{#gh-spin-button action="confirm" class="btn btn-green" submitting=submitting}}添加{{/gh-spin-button}}
</div>
